@model IEnumerable<Employee>
@{
    Layout = "~/Views/Shared/_LabManagerLayout.cshtml";
    ViewData["Title"] = "Inactive Technicians";
}

<div class="page-header">
    <div class="page-header-left">
        <h1>Inactive Lab Technicians</h1>
        <div class="page-sub">Deactivated technician accounts</div>
    </div>
    <a asp-action="Technicians" class="btn btn-ghost btn-sm">
        <i class="fas fa-arrow-left"></i> Back to Active
    </a>
</div>

<div class="card">
    @if (!Model.Any())
    {
        <div class="empty-state">
            <div class="empty-icon"><i class="fas fa-user-slash"></i></div>
            <h3>No inactive technicians</h3>
        </div>
    }
    else
    {
        <div class="table-wrap">
            <table>
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>SA ID Number</th>
                        <th>Email</th>
                        <th style="width:100px;">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var tech in Model)
                    {
                        <tr>
                            <td>@tech.FirstName @tech.LastName</td>
                            <td>@tech.SAIDNumber</td>
                            <td>@tech.Email</td>
                            <td>
                                <form asp-action="RestoreTechnician" method="post">
                                    <input type="hidden" name="id" value="@tech.Id" />
                                    <button type="submit" class="btn btn-warning btn-sm">
                                        <i class="fas fa-rotate-left"></i> Restore
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</div>